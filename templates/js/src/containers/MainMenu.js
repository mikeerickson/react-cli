import React, { Component } from 'react';import { Navbar, Nav, NavItem, NavDropdown, MenuItem, Collapse } from 'react-bootstrap';import { LinkContainer } from 'react-router-bootstrap';import ReactCSSTransitionGroup from 'react-addons-css-transition-group';import { getCookie } from '../tools/auth';import { connect } from 'react-redux';import { pushState } from 'redux-router';class MainMenu extends Component {        constructor(props, context) {        super(props, context);        this.state = {            aliasName: getCookie('cas_user')        };    }        handleLogout(){        location.href = "/logout";    }        render() {        return(            <div>                <Navbar inverse staticTop fixedTop fluid>                    <Navbar.Header>                        <Navbar.Brand>                            <a href="/home" style={{fontSize: 36, marginLeft: 30}}>深蓝</a>                        </Navbar.Brand>                        <Navbar.Toggle />                    </Navbar.Header>                    <Navbar.Collapse>                        <Nav pullRight role={1}>                            <LinkContainer to="/searcher"><NavItem eventKey={1}>用户检索</NavItem></LinkContainer>                            <LinkContainer to="/configer"><NavItem eventKey={2}>资源配置</NavItem></LinkContainer>                            <NavDropdown eventKey={2} title={"你好, " + this.state.aliasName} id="user">                                <MenuItem eventKey={2.1}>主页</MenuItem>                                <MenuItem divider />                                <NavItem eventKey={2.2} onSelect={this.handleLogout}>退出</NavItem>                            </NavDropdown>                        </Nav>                    </Navbar.Collapse>                </Navbar>                <div style={{marginTop: 60}}>                    {this.props.children}                </div>            </div>        );    }}export default connect(    state => ({}))(MainMenu)