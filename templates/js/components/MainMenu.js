import React, { Component } from 'react';import { Navbar, Nav, NavItem, NavDropdown, MenuItem } from 'react-bootstrap';import { LinkContainer } from 'react-router-bootstrap';import { getCookie } from '../tools/auth';class MainMenu extends Component {        constructor(props, context) {        super(props, context);        this.state = {            aliasName: getCookie('cas_user')        };    }        handleLogout(){        location.href = "/logout";    }        render() {        return(            <div>                <Navbar inverse staticTop fixedTop>                    <Navbar.Header>                        <Navbar.Brand>                            <a href="#">测试</a>                        </Navbar.Brand>                        <Navbar.Toggle />                    </Navbar.Header>                    <Navbar.Collapse>                        <Nav>                            <LinkContainer to="/comptest"><NavItem eventKey={1}>组件测试</NavItem></LinkContainer>                        </Nav>                        <Nav pullRight>                            <NavDropdown eventKey={2} title={"你好, " + this.state.aliasName} id="user">                                <MenuItem eventKey={2.1}>主页</MenuItem>                                <MenuItem divider />                                <NavItem eventKey={2.2} onSelect={this.handleLogout}>退出</NavItem>                            </NavDropdown>                        </Nav>                    </Navbar.Collapse>                </Navbar>                <div style={{marginTop: 10}}>                    {this.props.children}                </div>            </div>        );    }}export default MainMenu;